---
phase: 03-public-live-data-access
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/RCDragLiveServer/Controllers/PublicLiveController.cs
  - scripts/verify-phase3-public-endpoints.ps1
autonomous: true
requirements:
  - API-04
  - API-05
must_haves:
  truths:
    - "GET /health returns 200 with the minimal public JSON body {\"status\":\"healthy\"}."
    - "GET /health stays healthy even when no live race data has been posted yet."
    - "A single automated smoke check proves both GET /api/live and GET /health are public and emit no-cache headers."
  artifacts:
    - path: "src/RCDragLiveServer/Controllers/PublicLiveController.cs"
      provides: "Health endpoint plus final public endpoint wiring."
    - path: "scripts/verify-phase3-public-endpoints.ps1"
      provides: "End-to-end verification for live and health public behavior."
  key_links:
    - from: "src/RCDragLiveServer/Controllers/PublicLiveController.cs"
      to: "/health"
      via: "HttpGet route mapping"
      pattern: "health|\\[Route\\(\"health\"\\)\\]"
    - from: "scripts/verify-phase3-public-endpoints.ps1"
      to: "http://127.0.0.1:5099/api/live"
      via: "Invoke-WebRequest checks"
      pattern: "api/live"
    - from: "scripts/verify-phase3-public-endpoints.ps1"
      to: "http://127.0.0.1:5099/health"
      via: "Invoke-WebRequest checks"
      pattern: "/health"
---

<objective>
Add the public health contract and verify the Phase 3 endpoints end-to-end.

Purpose: Finish the operational/public-read surface for Render monitoring and spectator access with one-command automated proof.
Output: A public `GET /health` endpoint plus a Phase 3 PowerShell smoke script covering `/api/live` and `/health`.
</objective>

<execution_context>
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-public-live-data-access/03-CONTEXT.md
@.planning/phases/03-public-live-data-access/03-01-PLAN.md
@.planning/phases/02-secure-live-update-ingestion/02-03-PLAN.md
@src/RCDragLiveServer/Controllers/PublicLiveController.cs
@scripts/fixtures/live-update.valid.json

<interfaces>
Locked Phase 3 contracts:

```text
GET /health
200 OK
{"status":"healthy"}
Public access, no API key, no cache

GET /api/live
200 OK
Raw LiveRaceState JSON
Public access, no API key, no cache
```

Existing verification asset to reuse:

```text
scripts/fixtures/live-update.valid.json
POST /api/update with X-API-KEY seeds the in-memory store for smoke checks.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the public controller with the health endpoint</name>
  <read_first>.planning/phases/03-public-live-data-access/03-CONTEXT.md, .planning/phases/03-public-live-data-access/03-01-PLAN.md, src/RCDragLiveServer/Controllers/PublicLiveController.cs</read_first>
  <files>src/RCDragLiveServer/Controllers/PublicLiveController.cs</files>
  <action>Update `PublicLiveController` to also expose public `GET /health`. Return exactly the minimal JSON body `{ status = "healthy" }` with `200 OK`, regardless of whether any live data exists. Apply the same explicit cache-suppression header behavior used for `GET /api/live`. Keep both endpoints in this controller unless the existing code forces a cleaner split; do not add diagnostics, version data, or any API-key requirement.</action>
  <verify>
    <automated>dotnet build .\src\RCDragLiveServer\RCDragLiveServer.csproj</automated>
  </verify>
  <acceptance_criteria>The application exposes a public liveness endpoint at `/health` with the exact locked minimal response body and the same deterministic no-cache behavior as `/api/live`, without coupling health to the presence of seeded race data.</acceptance_criteria>
  <done>The service now contains both public read endpoints required for Phase 3.</done>
</task>

<task type="auto">
  <name>Task 2: Create and run a public endpoint smoke test</name>
  <read_first>src/RCDragLiveServer/Controllers/PublicLiveController.cs, .planning/phases/02-secure-live-update-ingestion/02-03-PLAN.md, scripts/fixtures/live-update.valid.json</read_first>
  <files>scripts/verify-phase3-public-endpoints.ps1</files>
  <action>Create `scripts/verify-phase3-public-endpoints.ps1` that starts the API project on `http://127.0.0.1:5099` with `$env:ApiKey = 'phase3-test-key'`, waits for readiness, and performs these checks in order: 1. `GET /health` without any auth header returns `200` and JSON body `{"status":"healthy"}`. 2. `GET /api/live` before seeding returns `200`, a JSON object matching the raw `LiveRaceState` shape, and `matches` as an empty array. 3. `POST /api/update` using the existing valid Phase 2 fixture and `X-API-KEY: phase3-test-key` succeeds so the in-memory store is seeded. 4. `GET /api/live` again without auth returns `200` and includes the seeded fixture values. 5. Both `/health` and `/api/live` responses include explicit cache-suppression headers that prove no-cache/no-store intent. Ensure the script always tears down the app process and exits non-zero on any assertion failure.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-phase3-public-endpoints.ps1</automated>
  </verify>
  <acceptance_criteria>One command proves both Phase 3 endpoints are public, return the locked response bodies, preserve the default empty-state behavior for `/api/live`, and emit cache-suppression headers before and after a seeded update.</acceptance_criteria>
  <done>Phase 3 public data access is end-to-end verifiable without relying on the out-of-scope test project.</done>
</task>

</tasks>

<verification>
Run the Phase 3 smoke test:
`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-phase3-public-endpoints.ps1`
</verification>

<success_criteria>
The service exposes public `GET /health` and `GET /api/live` endpoints that do not require authentication, return the locked minimal/default contracts, and are covered by a one-command end-to-end verification script.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-live-data-access/03-02-SUMMARY.md`
</output>
