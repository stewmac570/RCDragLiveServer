---
phase: 03-public-live-data-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/RCDragLiveServer/Controllers/PublicLiveController.cs
autonomous: true
requirements:
  - API-04
must_haves:
  truths:
    - "Anyone can call GET /api/live without an API key and receive the latest live race state as raw JSON."
    - "Before any update is posted, GET /api/live still returns 200 with the default empty LiveRaceState contract."
    - "GET /api/live explicitly disables caching so spectators do not reuse stale race state."
  artifacts:
    - path: "src/RCDragLiveServer/Controllers/PublicLiveController.cs"
      provides: "Public read endpoint for the latest live state."
  key_links:
    - from: "src/RCDragLiveServer/Controllers/PublicLiveController.cs"
      to: "src/RCDragLiveServer/Services/ILiveRaceStateStore.cs"
      via: "ILiveRaceStateStore.GetLatest()"
      pattern: "GetLatest\\("
    - from: "src/RCDragLiveServer/Controllers/PublicLiveController.cs"
      to: "/api/live"
      via: "HttpGet route mapping"
      pattern: "api/live|\\[Route\\(\"api/live\"\\)\\]"
---

<objective>
Create the public JSON endpoint for current live race state.

Purpose: Fulfill the spectator-facing API contract for Phase 3 without changing the payload shape or introducing auth.
Output: A controller endpoint at `GET /api/live` that returns raw `LiveRaceState` JSON with explicit no-cache behavior.
</objective>

<execution_context>
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-public-live-data-access/03-CONTEXT.md
@.planning/phases/02-secure-live-update-ingestion/02-02-SUMMARY.md
@src/RCDragLiveServer/Program.cs
@src/RCDragLiveServer/Models/LiveRaceState.cs
@src/RCDragLiveServer/Services/ILiveRaceStateStore.cs
@src/RCDragLiveServer/Controllers/LiveUpdateController.cs

<interfaces>
Existing contracts and patterns:

```csharp
public interface ILiveRaceStateStore
{
    LiveRaceState GetLatest();
    void SetLatest(LiveRaceState state);
}

public class LiveRaceState
{
    public string EventName { get; set; } = string.Empty;
    public string EventDate { get; set; } = string.Empty;
    public string CurrentRound { get; set; } = string.Empty;
    public string NextUp { get; set; } = string.Empty;
    public List<LiveMatch> Matches { get; set; } = new();
}
```

```text
Locked Phase 3 decisions:
- GET /api/live returns raw LiveRaceState JSON with 200 OK even before data exists.
- No wrapper object, no hasData flag, no metadata fields.
- GET /api/live is public and must send explicit no-cache/no-store headers.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the public live-state controller endpoint</name>
  <read_first>.planning/phases/03-public-live-data-access/03-CONTEXT.md, src/RCDragLiveServer/Program.cs, src/RCDragLiveServer/Services/ILiveRaceStateStore.cs, src/RCDragLiveServer/Controllers/LiveUpdateController.cs</read_first>
  <files>src/RCDragLiveServer/Controllers/PublicLiveController.cs</files>
  <action>Create `PublicLiveController` using the same ASP.NET Core controller style already established in `LiveUpdateController`. Add a public `GET /api/live` action that injects `ILiveRaceStateStore`, calls `GetLatest()`, and returns that object directly with `200 OK`. Do not add `[RequireApiKey]`, wrapper envelopes, status flags, timestamps, or query parameters. Apply explicit no-cache headers on the response inside this endpoint so the contract clearly signals `no-store` / `no-cache` behavior for spectator clients.</action>
  <verify>
    <automated>dotnet build .\src\RCDragLiveServer\RCDragLiveServer.csproj</automated>
  </verify>
  <acceptance_criteria>`GET /api/live` exists as a public controller action, returns the store's current `LiveRaceState` object without reshaping it, and sets deterministic cache-suppression headers on every response. The implementation preserves the Phase 1 default-empty-state behavior by relying on the existing singleton store default.</acceptance_criteria>
  <done>The codebase contains a concrete public read endpoint for live race JSON that satisfies the locked Phase 3 response shape and no-auth rules.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the endpoint compiles against the existing hosting setup</name>
  <read_first>src/RCDragLiveServer/Controllers/PublicLiveController.cs, src/RCDragLiveServer/Program.cs</read_first>
  <files>src/RCDragLiveServer/Controllers/PublicLiveController.cs</files>
  <action>Build only the API project after adding the controller. If the build surfaces namespace, route, or ASP.NET Core API shape issues, fix them within `PublicLiveController.cs` without broad refactors. Keep the implementation isolated to the new public-read controller and reuse the existing controller registration from `Program.cs` rather than changing startup shape.</action>
  <verify>
    <automated>dotnet build .\src\RCDragLiveServer\RCDragLiveServer.csproj</automated>
  </verify>
  <acceptance_criteria>The API project builds cleanly with the new controller in place and no additional hosting or dependency changes are required for `GET /api/live` to be active.</acceptance_criteria>
  <done>The live endpoint is implementation-ready for end-to-end public verification in the next plan.</done>
</task>

</tasks>

<verification>
Build the API project:
`dotnet build .\src\RCDragLiveServer\RCDragLiveServer.csproj`
</verification>

<success_criteria>
Phase 3 has a concrete public `GET /api/live` endpoint that returns raw `LiveRaceState` JSON, remains unauthenticated, preserves the default empty-state response, and compiles cleanly inside the existing ASP.NET Core service.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-live-data-access/03-01-SUMMARY.md`
</output>
