---
phase: 01-foundation-and-contracts
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/RCDragLiveServer/Program.cs
  - src/RCDragLiveServer/appsettings.json
  - src/RCDragLiveServer/Services/ILiveRaceStateStore.cs
  - src/RCDragLiveServer/Services/InMemoryLiveRaceStateStore.cs
  - tests/RCDragLiveServer.Tests/Services/InMemoryLiveRaceStateStoreTests.cs
autonomous: true
requirements:
  - HOST-02
  - HOST-03
must_haves:
  truths:
    - "The app can bind to the `PORT` environment variable when Render provides one."
    - "The service stays stateless apart from a singleton in-memory latest live state store."
    - "The in-memory state store returns a default empty `LiveRaceState` before any updates."
  artifacts:
    - path: "src/RCDragLiveServer/Program.cs"
      provides: "Render-friendly hosting and service registration"
    - path: "src/RCDragLiveServer/appsettings.json"
      provides: "Base configuration values for hosting and future settings"
    - path: "src/RCDragLiveServer/Services/ILiveRaceStateStore.cs"
      provides: "Explicit abstraction for latest-state access"
    - path: "src/RCDragLiveServer/Services/InMemoryLiveRaceStateStore.cs"
      provides: "Thread-safe in-memory latest-state implementation"
    - path: "tests/RCDragLiveServer.Tests/Services/InMemoryLiveRaceStateStoreTests.cs"
      provides: "Automated proof of empty-default and replace-latest behavior"
  key_links:
    - from: "src/RCDragLiveServer/Program.cs"
      to: "src/RCDragLiveServer/Services/InMemoryLiveRaceStateStore.cs"
      via: "singleton DI registration"
      pattern: "AddSingleton<ILiveRaceStateStore, InMemoryLiveRaceStateStore>"
    - from: "src/RCDragLiveServer/Program.cs"
      to: "Render port environment"
      via: "UseUrls with PORT"
      pattern: "PORT"
    - from: "src/RCDragLiveServer/Services/InMemoryLiveRaceStateStore.cs"
      to: "src/RCDragLiveServer/Models/LiveRaceState.cs"
      via: "latest state storage"
      pattern: "LiveRaceState"
---

<objective>
Wire the Phase 1 contracts into a Render-ready startup path and the only allowed runtime state: the latest in-memory live race payload.

Purpose: Later API and UI phases need a host that honors Render’s port contract and a central state store they can read from and update.
Output: `PORT` binding logic, singleton DI registration, app settings, and a tested in-memory `ILiveRaceStateStore`.
</objective>

<execution_context>
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-contracts/01-02-PLAN.md

<interfaces>
From `src/RCDragLiveServer/Models/LiveMatch.cs`:
```csharp
public class LiveMatch
{
    public string Driver1 { get; set; } = string.Empty;
    public string Driver2 { get; set; } = string.Empty;
}
```

From `src/RCDragLiveServer/Models/LiveRaceState.cs`:
```csharp
public class LiveRaceState
{
    public string EventName { get; set; } = string.Empty;
    public string EventDate { get; set; } = string.Empty;
    public string CurrentRound { get; set; } = string.Empty;
    public string NextUp { get; set; } = string.Empty;
    public List<LiveMatch> Matches { get; set; } = new();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a tested in-memory latest-state abstraction</name>
  <read_first>src/RCDragLiveServer/Services/ILiveRaceStateStore.cs, src/RCDragLiveServer/Services/InMemoryLiveRaceStateStore.cs, tests/RCDragLiveServer.Tests/Services/InMemoryLiveRaceStateStoreTests.cs, src/RCDragLiveServer/Models/LiveRaceState.cs, .planning/phases/01-foundation-and-contracts/01-CONTEXT.md</read_first>
  <files>src/RCDragLiveServer/Services/ILiveRaceStateStore.cs, src/RCDragLiveServer/Services/InMemoryLiveRaceStateStore.cs, tests/RCDragLiveServer.Tests/Services/InMemoryLiveRaceStateStoreTests.cs</files>
  <behavior>
    - Test 1: A fresh store returns a non-null `LiveRaceState` whose `Matches.Count` is `0`.
    - Test 2: Calling `SetLatest` replaces the stored state returned by `GetLatest`.
  </behavior>
  <action>Create `src/RCDragLiveServer/Services/ILiveRaceStateStore.cs` with two exact members: `LiveRaceState GetLatest();` and `void SetLatest(LiveRaceState state);`. Create `src/RCDragLiveServer/Services/InMemoryLiveRaceStateStore.cs` implementing that interface with a private field initialized to `new LiveRaceState()` and a private lock object so `GetLatest` and `SetLatest` are thread-safe. Create `tests/RCDragLiveServer.Tests/Services/InMemoryLiveRaceStateStoreTests.cs` with xUnit tests that assert the default state is non-null with empty `Matches`, and that `SetLatest` causes `GetLatest().EventName` and `GetLatest().NextUp` to return the replacement values.</action>
  <acceptance_criteria>`src/RCDragLiveServer/Services/ILiveRaceStateStore.cs` contains `LiveRaceState GetLatest();`; `src/RCDragLiveServer/Services/ILiveRaceStateStore.cs` contains `void SetLatest(LiveRaceState state);`; `src/RCDragLiveServer/Services/InMemoryLiveRaceStateStore.cs` contains `new LiveRaceState()`; `tests/RCDragLiveServer.Tests/Services/InMemoryLiveRaceStateStoreTests.cs` contains `SetLatest`; `tests/RCDragLiveServer.Tests/Services/InMemoryLiveRaceStateStoreTests.cs` contains `GetLatest`; `dotnet test RCDragLiveServer.sln --filter InMemoryLiveRaceStateStoreTests` succeeds.</acceptance_criteria>
  <verify>
    <automated>dotnet test RCDragLiveServer.sln --filter InMemoryLiveRaceStateStoreTests</automated>
  </verify>
  <done>The only allowed runtime state mechanism exists, is explicit, and is covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add Render `PORT` binding and register the in-memory store in startup</name>
  <read_first>src/RCDragLiveServer/Program.cs, src/RCDragLiveServer/appsettings.json, src/RCDragLiveServer/Services/ILiveRaceStateStore.cs, src/RCDragLiveServer/Services/InMemoryLiveRaceStateStore.cs, .planning/PROJECT.md</read_first>
  <files>src/RCDragLiveServer/Program.cs, src/RCDragLiveServer/appsettings.json</files>
  <action>Update `src/RCDragLiveServer/Program.cs` to read `var portValue = Environment.GetEnvironmentVariable("PORT");` before building the app. If `portValue` is not null or whitespace, parse it with `int.TryParse` and call `builder.WebHost.UseUrls($"http://0.0.0.0:{port}")`. Register the store with the exact statement `builder.Services.AddSingleton<ILiveRaceStateStore, InMemoryLiveRaceStateStore>();`. Keep `AddControllers()` and `MapControllers()` in place. Create `src/RCDragLiveServer/appsettings.json` with a minimal JSON object containing `"Logging"` and `"AllowedHosts": "*"`. Do not add database connection strings or persistent cache settings.</action>
  <acceptance_criteria>`src/RCDragLiveServer/Program.cs` contains `Environment.GetEnvironmentVariable(\"PORT\")`; `src/RCDragLiveServer/Program.cs` contains `UseUrls($\"http://0.0.0.0:{port}\")`; `src/RCDragLiveServer/Program.cs` contains `AddSingleton<ILiveRaceStateStore, InMemoryLiveRaceStateStore>()`; `src/RCDragLiveServer/appsettings.json` contains `"AllowedHosts": "*"`; `dotnet test RCDragLiveServer.sln` succeeds.</acceptance_criteria>
  <verify>
    <automated>dotnet test RCDragLiveServer.sln</automated>
  </verify>
  <done>The app is Render-ready and centrally exposes an in-memory latest-state store through DI.</done>
</task>

</tasks>

<verification>
The app still builds and all tests pass after startup wiring, proving that the only retained runtime state is the tested in-memory latest payload and that Render port binding logic is present.
</verification>

<success_criteria>
Phase 1 ends with a buildable ASP.NET Core app, stable live race contracts, and a Render-compatible startup path backed only by an in-memory latest-state store.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-contracts/01-03-SUMMARY.md`
</output>
