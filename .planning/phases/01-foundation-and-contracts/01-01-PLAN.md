---
phase: 01-foundation-and-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - RCDragLiveServer.sln
  - src/RCDragLiveServer/RCDragLiveServer.csproj
  - src/RCDragLiveServer/Program.cs
  - src/RCDragLiveServer/Properties/launchSettings.json
autonomous: true
requirements:
  - HOST-01
must_haves:
  truths:
    - "The service builds and runs as an ASP.NET Core Web API targeting .NET 8."
    - "The entrypoint is minimal-hosting based and ready for later API and page endpoints."
  artifacts:
    - path: "RCDragLiveServer.sln"
      provides: "Solution container for app and future test project"
    - path: "src/RCDragLiveServer/RCDragLiveServer.csproj"
      provides: "ASP.NET Core Web API project targeting net8.0"
    - path: "src/RCDragLiveServer/Program.cs"
      provides: "Minimal hosting entrypoint with controller and routing baseline"
    - path: "src/RCDragLiveServer/Properties/launchSettings.json"
      provides: "Local development launch profile"
  key_links:
    - from: "RCDragLiveServer.sln"
      to: "src/RCDragLiveServer/RCDragLiveServer.csproj"
      via: "solution project reference"
      pattern: "Project\\("
    - from: "src/RCDragLiveServer/Program.cs"
      to: "src/RCDragLiveServer/RCDragLiveServer.csproj"
      via: "ASP.NET Core Web API startup"
      pattern: "WebApplication\\.CreateBuilder"
---

<objective>
Create the ASP.NET Core Web API skeleton that the rest of Phase 1 will extend.

Purpose: Establish the actual .NET 8 application baseline instead of planning against a notional project.
Output: A buildable solution, Web API project, minimal-hosting entrypoint, and local launch profile.
</objective>

<execution_context>
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-contracts/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the solution and .NET 8 Web API project baseline</name>
  <read_first>RCDragLiveServer.sln, src/RCDragLiveServer/RCDragLiveServer.csproj, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-foundation-and-contracts/01-CONTEXT.md</read_first>
  <files>RCDragLiveServer.sln, src/RCDragLiveServer/RCDragLiveServer.csproj</files>
  <action>Create a solution file named `RCDragLiveServer.sln` and add a Web API project at `src/RCDragLiveServer/RCDragLiveServer.csproj`. The project must use `Sdk="Microsoft.NET.Sdk.Web"`, set `<TargetFramework>net8.0</TargetFramework>`, `<Nullable>enable</Nullable>`, and `<ImplicitUsings>enable</ImplicitUsings>`. Do not add extra packages or persistence libraries. Keep the project name `RCDragLiveServer` and ensure the solution references that project.</action>
  <acceptance_criteria>`RCDragLiveServer.sln` exists; `src/RCDragLiveServer/RCDragLiveServer.csproj` contains `<TargetFramework>net8.0</TargetFramework>`; `src/RCDragLiveServer/RCDragLiveServer.csproj` contains `Microsoft.NET.Sdk.Web`; `dotnet sln RCDragLiveServer.sln list` includes `src\\RCDragLiveServer\\RCDragLiveServer.csproj`.</acceptance_criteria>
  <verify>
    <automated>dotnet sln RCDragLiveServer.sln list</automated>
  </verify>
  <done>The repository contains a real .NET 8 ASP.NET Core Web API project wired into the solution.</done>
</task>

<task type="auto">
  <name>Task 2: Add the minimal hosting entrypoint and local launch profile</name>
  <read_first>src/RCDragLiveServer/Program.cs, src/RCDragLiveServer/Properties/launchSettings.json, src/RCDragLiveServer/RCDragLiveServer.csproj, .planning/phases/01-foundation-and-contracts/01-CONTEXT.md</read_first>
  <files>src/RCDragLiveServer/Program.cs, src/RCDragLiveServer/Properties/launchSettings.json</files>
  <action>Create `src/RCDragLiveServer/Program.cs` using the minimal hosting model with these concrete statements: `var builder = WebApplication.CreateBuilder(args);`, `builder.Services.AddControllers();`, `builder.Services.AddEndpointsApiExplorer();`, `var app = builder.Build();`, `app.MapControllers();`, and `app.Run();`. Create `src/RCDragLiveServer/Properties/launchSettings.json` with an HTTP profile bound to `http://localhost:5000` and environment `ASPNETCORE_ENVIRONMENT` set to `Development`. Do not add Swagger, authentication, or database code in this phase.</action>
  <acceptance_criteria>`src/RCDragLiveServer/Program.cs` contains `builder.Services.AddControllers();`; `src/RCDragLiveServer/Program.cs` contains `app.MapControllers();`; `src/RCDragLiveServer/Program.cs` contains `app.Run();`; `src/RCDragLiveServer/Properties/launchSettings.json` contains `http://localhost:5000`; `dotnet build RCDragLiveServer.sln` succeeds.</acceptance_criteria>
  <verify>
    <automated>dotnet build RCDragLiveServer.sln</automated>
  </verify>
  <done>The Web API app starts from a minimal `Program.cs` and has a reproducible local launch profile.</done>
</task>

</tasks>

<verification>
`dotnet build RCDragLiveServer.sln` completes without errors and the solution contains a single buildable ASP.NET Core Web API project targeting .NET 8.
</verification>

<success_criteria>
Phase executors can build the repository and extend the app using the standard ASP.NET Core minimal-hosting entrypoint without first creating infrastructure from scratch.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-contracts/01-01-SUMMARY.md`
</output>
