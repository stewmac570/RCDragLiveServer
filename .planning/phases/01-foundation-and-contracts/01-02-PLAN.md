---
phase: 01-foundation-and-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/RCDragLiveServer/Models/LiveMatch.cs
  - src/RCDragLiveServer/Models/LiveRaceState.cs
  - tests/RCDragLiveServer.Tests/RCDragLiveServer.Tests.csproj
  - tests/RCDragLiveServer.Tests/Models/LiveRaceStateContractTests.cs
  - RCDragLiveServer.sln
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
must_haves:
  truths:
    - "The codebase defines a concrete `LiveRaceState` model for the latest live event state."
    - "The codebase defines a concrete `LiveMatch` model for current matchup entries."
    - "`LiveRaceState` includes event name, event date, current round, next-up race, and a match list with default empty collection behavior."
  artifacts:
    - path: "src/RCDragLiveServer/Models/LiveMatch.cs"
      provides: "Live match contract with driver display fields"
    - path: "src/RCDragLiveServer/Models/LiveRaceState.cs"
      provides: "Top-level live race payload contract"
    - path: "tests/RCDragLiveServer.Tests/Models/LiveRaceStateContractTests.cs"
      provides: "Automated proof of required fields and defaults"
  key_links:
    - from: "src/RCDragLiveServer/Models/LiveRaceState.cs"
      to: "src/RCDragLiveServer/Models/LiveMatch.cs"
      via: "Matches collection typed as LiveMatch"
      pattern: "List<LiveMatch>|IReadOnlyList<LiveMatch>"
    - from: "tests/RCDragLiveServer.Tests/Models/LiveRaceStateContractTests.cs"
      to: "src/RCDragLiveServer/Models/LiveRaceState.cs"
      via: "contract assertions"
      pattern: "LiveRaceState"
---

<objective>
Define the v1 live payload contracts exactly once and lock them behind automated tests.

Purpose: Later API and UI phases need a stable, simple contract that matches the user’s locked decisions from `01-CONTEXT.md`.
Output: `LiveRaceState`, `LiveMatch`, and tests that prove the required fields and defaults.
</objective>

<execution_context>
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-contracts/01-CONTEXT.md

<interfaces>
No existing runtime interfaces exist yet. This plan creates the canonical Phase 1 model contracts that later plans must consume directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests that lock the v1 live payload contract</name>
  <read_first>tests/RCDragLiveServer.Tests/RCDragLiveServer.Tests.csproj, tests/RCDragLiveServer.Tests/Models/LiveRaceStateContractTests.cs, src/RCDragLiveServer/Models/LiveRaceState.cs, src/RCDragLiveServer/Models/LiveMatch.cs, .planning/REQUIREMENTS.md, .planning/phases/01-foundation-and-contracts/01-CONTEXT.md</read_first>
  <files>tests/RCDragLiveServer.Tests/RCDragLiveServer.Tests.csproj, tests/RCDragLiveServer.Tests/Models/LiveRaceStateContractTests.cs, RCDragLiveServer.sln</files>
  <behavior>
    - Test 1: A new `LiveRaceState` instance exposes `EventName`, `EventDate`, `CurrentRound`, `NextUp`, and `Matches`.
    - Test 2: `Matches` defaults to an empty collection instead of `null`.
    - Test 3: A `LiveMatch` instance exposes `Driver1` and `Driver2` as string properties.
  </behavior>
  <action>Create an xUnit test project at `tests/RCDragLiveServer.Tests/RCDragLiveServer.Tests.csproj` targeting `net8.0` with package references `Microsoft.NET.Test.Sdk`, `xunit`, `xunit.runner.visualstudio`, and `coverlet.collector`. Reference `src/RCDragLiveServer/RCDragLiveServer.csproj` from the test project and add the test project to `RCDragLiveServer.sln`. Create `tests/RCDragLiveServer.Tests/Models/LiveRaceStateContractTests.cs` with tests that assert the exact public property names `EventName`, `EventDate`, `CurrentRound`, `NextUp`, `Matches`, `Driver1`, and `Driver2`, and assert a new `LiveRaceState().Matches.Count` equals `0`.</action>
  <acceptance_criteria>`tests/RCDragLiveServer.Tests/RCDragLiveServer.Tests.csproj` exists; it contains `xunit`; `RCDragLiveServer.sln` lists `tests\\RCDragLiveServer.Tests\\RCDragLiveServer.Tests.csproj`; `tests/RCDragLiveServer.Tests/Models/LiveRaceStateContractTests.cs` contains `EventName`; it contains `EventDate`; it contains `CurrentRound`; it contains `NextUp`; it contains `Driver1`; it contains `Driver2`.</acceptance_criteria>
  <verify>
    <automated>dotnet test RCDragLiveServer.sln --filter LiveRaceStateContractTests</automated>
  </verify>
  <done>A failing-then-passing test harness exists that encodes the exact Phase 1 model contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the `LiveMatch` and `LiveRaceState` models to satisfy the contract</name>
  <read_first>src/RCDragLiveServer/Models/LiveMatch.cs, src/RCDragLiveServer/Models/LiveRaceState.cs, tests/RCDragLiveServer.Tests/Models/LiveRaceStateContractTests.cs, .planning/phases/01-foundation-and-contracts/01-CONTEXT.md</read_first>
  <files>src/RCDragLiveServer/Models/LiveMatch.cs, src/RCDragLiveServer/Models/LiveRaceState.cs</files>
  <action>Create `src/RCDragLiveServer/Models/LiveMatch.cs` as a public class with string properties `Driver1` and `Driver2`, both initialized to `string.Empty`. Create `src/RCDragLiveServer/Models/LiveRaceState.cs` as a public class with string properties `EventName`, `EventDate`, `CurrentRound`, and `NextUp`, each initialized to `string.Empty`, plus a `List<LiveMatch>` property named `Matches` initialized with `new()`. Keep the model display-oriented and do not add IDs, timestamps, timezone parsing, validation attributes, or version fields.</action>
  <acceptance_criteria>`src/RCDragLiveServer/Models/LiveMatch.cs` contains `public string Driver1`; `src/RCDragLiveServer/Models/LiveMatch.cs` contains `public string Driver2`; `src/RCDragLiveServer/Models/LiveRaceState.cs` contains `public string EventName`; `src/RCDragLiveServer/Models/LiveRaceState.cs` contains `public string EventDate`; `src/RCDragLiveServer/Models/LiveRaceState.cs` contains `public string CurrentRound`; `src/RCDragLiveServer/Models/LiveRaceState.cs` contains `public string NextUp`; `src/RCDragLiveServer/Models/LiveRaceState.cs` contains `List<LiveMatch> Matches`; `dotnet test RCDragLiveServer.sln --filter LiveRaceStateContractTests` succeeds.</acceptance_criteria>
  <verify>
    <automated>dotnet test RCDragLiveServer.sln --filter LiveRaceStateContractTests</automated>
  </verify>
  <done>The model layer matches the locked v1 contract exactly and is covered by automated tests.</done>
</task>

</tasks>

<verification>
The test project runs successfully and proves the exact property set and default empty-list behavior required by Phase 1.
</verification>

<success_criteria>
Later plans can consume a stable `LiveRaceState`/`LiveMatch` contract without reopening data-shape decisions or guessing about defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-contracts/01-02-SUMMARY.md`
</output>
