---
phase: 02-secure-live-update-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/RCDragLiveServer/Program.cs
  - src/RCDragLiveServer/appsettings.json
  - src/RCDragLiveServer/Security/ApiKeyAuthorizationFilter.cs
  - src/RCDragLiveServer/Security/RequireApiKeyAttribute.cs
autonomous: true
requirements:
  - API-02
  - CFG-01
must_haves:
  truths:
    - "The app refuses to start when ApiKey configuration is missing or whitespace."
    - "Requests that omit X-API-KEY receive 401 with a generic unauthorized body."
    - "Requests with the wrong X-API-KEY receive the same 401 behavior as missing credentials."
  artifacts:
    - path: "src/RCDragLiveServer/Program.cs"
      provides: "Fail-fast ApiKey configuration loading and DI wiring for request validation."
    - path: "src/RCDragLiveServer/Security/ApiKeyAuthorizationFilter.cs"
      provides: "Reusable header-based API key guard that returns 401 JSON."
    - path: "src/RCDragLiveServer/Security/RequireApiKeyAttribute.cs"
      provides: "Controller/action attribute for attaching the API key guard."
  key_links:
    - from: "src/RCDragLiveServer/Program.cs"
      to: "IConfiguration['ApiKey']"
      via: "startup validation"
      pattern: "builder\\.Configuration\\[\"ApiKey\"\\]"
    - from: "src/RCDragLiveServer/Security/RequireApiKeyAttribute.cs"
      to: "src/RCDragLiveServer/Security/ApiKeyAuthorizationFilter.cs"
      via: "TypeFilterAttribute"
      pattern: "typeof\\(ApiKeyAuthorizationFilter\\)"
---

<objective>
Create the API key configuration baseline and reusable request guard for secure update ingestion.

Purpose: Lock in the phase's security boundary before any update endpoint is added.
Output: Startup config validation plus a reusable `X-API-KEY` authorization filter.
</objective>

<execution_context>
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-secure-live-update-ingestion/02-CONTEXT.md
@.planning/phases/01-foundation-and-contracts/01-03-SUMMARY.md
@src/RCDragLiveServer/Program.cs
@src/RCDragLiveServer/appsettings.json

<interfaces>
From `src/RCDragLiveServer/Program.cs`:

```csharp
builder.Services.AddControllers();
builder.Services.AddEndpointsApiExplorer();
builder.Services.AddSingleton<ILiveRaceStateStore, InMemoryLiveRaceStateStore>();

var app = builder.Build();
app.MapControllers();
```

Locked phase decisions from `02-CONTEXT.md`:

```text
POST /api/update requires X-API-KEY.
Missing key => 401 {"error":"unauthorized"}.
Invalid key => same 401 body.
Canonical config key = ApiKey.
Environment override key = ApiKey.
If ApiKey is missing at startup, app fails fast.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fail-fast ApiKey startup configuration</name>
  <read_first>.planning/phases/02-secure-live-update-ingestion/02-CONTEXT.md, src/RCDragLiveServer/Program.cs, src/RCDragLiveServer/appsettings.json</read_first>
  <files>src/RCDragLiveServer/Program.cs, src/RCDragLiveServer/appsettings.json</files>
  <action>Update `Program.cs` so startup reads `builder.Configuration["ApiKey"]`, trims it, and throws an `InvalidOperationException` with the concrete message `Configuration key 'ApiKey' is required.` when the value is null, empty, or whitespace. Keep the existing `PORT` binding and singleton `ILiveRaceStateStore` registration unchanged. Preserve the default ASP.NET Core config pipeline so `appsettings.json` and the `ApiKey` environment variable both remain valid sources. Update `appsettings.json` only if needed to keep the configuration surface obvious without introducing a usable default secret.</action>
  <verify>
    <automated>dotnet build src/RCDragLiveServer/RCDragLiveServer.csproj -nologo</automated>
  </verify>
  <acceptance_criteria>`Program.cs` validates `ApiKey` before `builder.Build()`, the validation message is exactly `Configuration key 'ApiKey' is required.`, and existing host/store registrations still compile.</acceptance_criteria>
  <done>The application cannot boot without an explicit ApiKey configuration value and still builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable X-API-KEY request guard</name>
  <read_first>.planning/phases/02-secure-live-update-ingestion/02-CONTEXT.md, src/RCDragLiveServer/Program.cs</read_first>
  <files>src/RCDragLiveServer/Program.cs, src/RCDragLiveServer/Security/ApiKeyAuthorizationFilter.cs, src/RCDragLiveServer/Security/RequireApiKeyAttribute.cs</files>
  <action>Create `ApiKeyAuthorizationFilter` under `src/RCDragLiveServer/Security/` implementing `IAsyncAuthorizationFilter`. Read the incoming `X-API-KEY` header only, compare it against the configured `ApiKey`, and on missing or mismatched values set `context.Result` to `new JsonResult(new { error = "unauthorized" }) { StatusCode = StatusCodes.Status401Unauthorized }`. Rely on normal ASP.NET Core header handling so header name casing stays case-insensitive, and do not inspect query-string or body values for fallback credentials. Create `RequireApiKeyAttribute` as a `TypeFilterAttribute` wrapper over the filter so controllers can opt in declaratively. Register any filter dependencies in `Program.cs` using the minimal built-in DI container.</action>
  <verify>
    <automated>dotnet build src/RCDragLiveServer/RCDragLiveServer.csproj -nologo</automated>
  </verify>
  <acceptance_criteria>The repo contains a reusable attribute/filter pair, both missing and invalid `X-API-KEY` cases produce the exact generic JSON body `{"error":"unauthorized"}`, and credential lookup is limited to the request header.</acceptance_criteria>
  <done>Phase 2 has a reusable authorization primitive that can protect `POST /api/update` without duplicating key-check logic in the controller.</done>
</task>

</tasks>

<verification>
Build the API project after startup validation and filter wiring changes:
`dotnet build src/RCDragLiveServer/RCDragLiveServer.csproj -nologo`
</verification>

<success_criteria>
The application has a concrete `ApiKey` startup contract and a reusable `X-API-KEY` guard that returns the locked 401 response shape for unauthorized requests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-secure-live-update-ingestion/02-01-SUMMARY.md`
</output>
