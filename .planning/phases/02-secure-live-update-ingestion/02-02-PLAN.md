---
phase: 02-secure-live-update-ingestion
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/RCDragLiveServer/Controllers/LiveUpdateController.cs
autonomous: true
requirements:
  - API-01
  - API-03
must_haves:
  truths:
    - "RC Drag Manager can POST the current live race payload to /api/update when the correct X-API-KEY is supplied."
    - "An accepted payload fully replaces the in-memory latest live race state."
    - "Payloads with null or missing Matches are rejected with 400 Bad Request."
  artifacts:
    - path: "src/RCDragLiveServer/Controllers/LiveUpdateController.cs"
      provides: "Protected POST /api/update endpoint that validates payload shape and stores the latest state."
  key_links:
    - from: "src/RCDragLiveServer/Controllers/LiveUpdateController.cs"
      to: "src/RCDragLiveServer/Security/RequireApiKeyAttribute.cs"
      via: "controller attribute"
      pattern: "\\[RequireApiKey\\]"
    - from: "src/RCDragLiveServer/Controllers/LiveUpdateController.cs"
      to: "src/RCDragLiveServer/Services/ILiveRaceStateStore.cs"
      via: "SetLatest call"
      pattern: "\\.SetLatest\\("
---

<objective>
Implement the protected live update endpoint that replaces the current in-memory race state.

Purpose: Deliver the actual Phase 2 ingestion path once configuration and auth primitives exist.
Output: A controller exposing `POST /api/update` with the locked request and response behavior.
</objective>

<execution_context>
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-secure-live-update-ingestion/02-CONTEXT.md
@.planning/phases/02-secure-live-update-ingestion/02-01-PLAN.md
@src/RCDragLiveServer/Models/LiveRaceState.cs
@src/RCDragLiveServer/Models/LiveMatch.cs
@src/RCDragLiveServer/Services/ILiveRaceStateStore.cs

<interfaces>
From `src/RCDragLiveServer/Models/LiveRaceState.cs`:

```csharp
public class LiveRaceState
{
    public string EventName { get; set; } = string.Empty;
    public string EventDate { get; set; } = string.Empty;
    public string CurrentRound { get; set; } = string.Empty;
    public string NextUp { get; set; } = string.Empty;
    public List<LiveMatch> Matches { get; set; } = new();
}
```

From `src/RCDragLiveServer/Services/ILiveRaceStateStore.cs`:

```csharp
public interface ILiveRaceStateStore
{
    LiveRaceState GetLatest();
    void SetLatest(LiveRaceState state);
}
```

Locked phase decisions from `02-CONTEXT.md`:

```text
Success => 200 OK with a minimal confirmation payload.
Invalid payload => 400 Bad Request.
Each accepted update fully replaces latest in-memory state.
If Matches is null/missing, reject with 400.
Required top-level string fields may be empty strings.
Unknown JSON fields are ignored.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the protected POST /api/update controller endpoint</name>
  <read_first>.planning/phases/02-secure-live-update-ingestion/02-CONTEXT.md, .planning/phases/02-secure-live-update-ingestion/02-01-PLAN.md, src/RCDragLiveServer/Models/LiveRaceState.cs, src/RCDragLiveServer/Services/ILiveRaceStateStore.cs</read_first>
  <files>src/RCDragLiveServer/Controllers/LiveUpdateController.cs</files>
  <action>Create `LiveUpdateController` under `src/RCDragLiveServer/Controllers/` as an `[ApiController]` with `[Route("api/update")]`. Add a single `[HttpPost]` action protected by `[RequireApiKey]` that binds the request body to `LiveRaceState`. Accept empty strings in the top-level string fields, but reject a null request body and reject any payload where `Matches` is null or omitted by returning `BadRequest(new { error = "invalid_payload" })`. Do not add alternate routes, query parameters, or diagnostics metadata. Keep the action response payload minimal and deterministic.</action>
  <verify>
    <automated>dotnet build src/RCDragLiveServer/RCDragLiveServer.csproj -nologo</automated>
  </verify>
  <acceptance_criteria>The controller exposes exactly one `POST /api/update` action, the action is protected by `[RequireApiKey]`, and null or missing `Matches` produces `400` with `{"error":"invalid_payload"}` instead of updating state.</acceptance_criteria>
  <done>The API surface for protected live updates exists with the locked route, auth hook, and payload validation boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the latest in-memory state and return minimal success JSON</name>
  <read_first>src/RCDragLiveServer/Controllers/LiveUpdateController.cs, src/RCDragLiveServer/Services/ILiveRaceStateStore.cs, src/RCDragLiveServer/Services/InMemoryLiveRaceStateStore.cs</read_first>
  <files>src/RCDragLiveServer/Controllers/LiveUpdateController.cs</files>
  <action>Finish the controller by injecting `ILiveRaceStateStore`, calling `SetLatest` with the accepted `LiveRaceState`, and returning `Ok(new { status = "updated" })` on success. Treat the posted object as the full replacement snapshot; do not merge with the existing state and do not mutate missing strings into errors. Keep JSON additive-compatible by relying on default ASP.NET Core JSON behavior so unknown incoming fields are ignored automatically.</action>
  <verify>
    <automated>dotnet build src/RCDragLiveServer/RCDragLiveServer.csproj -nologo</automated>
  </verify>
  <acceptance_criteria>Accepted requests overwrite the in-memory latest state through `ILiveRaceStateStore.SetLatest`, successful responses are `200 OK` with `{"status":"updated"}`, and the endpoint does not merge partial payloads with old state.</acceptance_criteria>
  <done>The service has a complete protected ingestion path that stores only the latest race snapshot in memory.</done>
</task>

</tasks>

<verification>
Build the API project after adding the controller:
`dotnet build src/RCDragLiveServer/RCDragLiveServer.csproj -nologo`
</verification>

<success_criteria>
`POST /api/update` exists, requires the API key filter from plan `02-01`, validates the `Matches` requirement, and replaces the singleton in-memory latest state on success.
</success_criteria>

<output>
After completion, create `.planning/phases/02-secure-live-update-ingestion/02-02-SUMMARY.md`
</output>
