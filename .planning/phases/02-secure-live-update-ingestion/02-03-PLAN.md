---
phase: 02-secure-live-update-ingestion
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - scripts/fixtures/live-update.valid.json
  - scripts/fixtures/live-update.invalid-missing-matches.json
  - scripts/verify-phase2-update-endpoint.ps1
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
must_haves:
  truths:
    - "Automated verification proves the happy path for POST /api/update."
    - "Automated verification proves missing and invalid API keys are rejected with the generic 401 body."
    - "Automated verification proves a payload with missing Matches is rejected with 400."
  artifacts:
    - path: "scripts/fixtures/live-update.valid.json"
      provides: "Known-good Phase 2 payload for endpoint verification."
    - path: "scripts/fixtures/live-update.invalid-missing-matches.json"
      provides: "Known-bad payload missing Matches to exercise the 400 branch."
    - path: "scripts/verify-phase2-update-endpoint.ps1"
      provides: "End-to-end automated smoke verification for secure update ingestion."
  key_links:
    - from: "scripts/verify-phase2-update-endpoint.ps1"
      to: "http://127.0.0.1:5099/api/update"
      via: "Invoke-RestMethod / Invoke-WebRequest calls"
      pattern: "api/update"
    - from: "scripts/verify-phase2-update-endpoint.ps1"
      to: "ApiKey"
      via: "process environment variable for app startup and request headers"
      pattern: "\\$env:ApiKey"
---

<objective>
Add concrete automated verification assets for Phase 2 endpoint behavior.

Purpose: Give execution a repeatable proof that auth, payload validation, and latest-state replacement all behave as planned without relying on the broken test project.
Output: JSON fixtures plus a PowerShell smoke-test script for `POST /api/update`.
</objective>

<execution_context>
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Stewart McMillan/source/repos/RCDragLiveServer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-secure-live-update-ingestion/02-CONTEXT.md
@.planning/phases/02-secure-live-update-ingestion/02-02-PLAN.md
@src/RCDragLiveServer/Controllers/LiveUpdateController.cs

<interfaces>
Endpoint contract to verify:

```text
POST http://127.0.0.1:5099/api/update
Header: X-API-KEY: phase2-test-key
Success: 200 {"status":"updated"}
Unauthorized: 401 {"error":"unauthorized"}
Invalid payload (missing Matches): 400 {"error":"invalid_payload"}
```

Execution constraint from project history:

```text
Do not rely on the existing test project; use API-project build/run verification instead.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fixed Phase 2 payload fixtures</name>
  <read_first>.planning/phases/02-secure-live-update-ingestion/02-CONTEXT.md, src/RCDragLiveServer/Models/LiveRaceState.cs</read_first>
  <files>scripts/fixtures/live-update.valid.json, scripts/fixtures/live-update.invalid-missing-matches.json</files>
  <action>Create `scripts/fixtures/live-update.valid.json` with these exact top-level keys: `eventName`, `eventDate`, `currentRound`, `nextUp`, and `matches`. Use the concrete values `Friday Night Shootout`, `2026-03-20`, `Round 3`, `Lane 1: Alice vs Bob`, and a `matches` array containing two objects: `{ "driver1": "Alice", "driver2": "Bob" }` and `{ "driver1": "Carol", "driver2": "Dan" }`. Create `scripts/fixtures/live-update.invalid-missing-matches.json` with the same string fields but omit the `matches` property entirely so it exercises the locked 400 path.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path 'scripts/fixtures/live-update.valid.json' -and Test-Path 'scripts/fixtures/live-update.invalid-missing-matches.json'"</automated>
  </verify>
  <acceptance_criteria>The repository contains one known-good update payload and one payload that is invalid specifically because `matches` is absent.</acceptance_criteria>
  <done>Phase 2 has stable payload fixtures that the smoke test can use without inline JSON duplication.</done>
</task>

<task type="auto">
  <name>Task 2: Create and run an end-to-end PowerShell smoke test</name>
  <read_first>.planning/phases/02-secure-live-update-ingestion/02-CONTEXT.md, src/RCDragLiveServer/Controllers/LiveUpdateController.cs, scripts/fixtures/live-update.valid.json, scripts/fixtures/live-update.invalid-missing-matches.json</read_first>
  <files>scripts/verify-phase2-update-endpoint.ps1</files>
  <action>Create `scripts/verify-phase2-update-endpoint.ps1` that starts the API project on `http://127.0.0.1:5099` with `$env:ApiKey = 'phase2-test-key'`, waits for the app to accept requests, then performs four checks in order: 1. POST the valid fixture with `X-API-KEY: phase2-test-key` and assert `200` plus response body `{"status":"updated"}`. 2. POST the same fixture without `X-API-KEY` and assert `401` plus `{"error":"unauthorized"}`. 3. POST the same fixture with `X-API-KEY: wrong-key` and assert the same `401` body. 4. POST the invalid fixture with `X-API-KEY: phase2-test-key` and assert `400` plus `{"error":"invalid_payload"}`. Ensure the script always stops the started app process before exiting and returns a non-zero exit code on any failed assertion.</action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-phase2-update-endpoint.ps1</automated>
  </verify>
  <acceptance_criteria>The script is self-contained, tears down the app process on both pass and fail paths, and exits successfully only when the success, unauthorized, and invalid-payload cases all match the locked Phase 2 response contracts.</acceptance_criteria>
  <done>Phase 2 endpoint behavior can be re-verified end-to-end with a single command and without the existing test project.</done>
</task>

</tasks>

<verification>
Run the smoke verification script:
`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-phase2-update-endpoint.ps1`
</verification>

<success_criteria>
There is a one-command automated proof for the Phase 2 happy path and locked failure responses, using the real API process and concrete payload fixtures.
</success_criteria>

<output>
After completion, create `.planning/phases/02-secure-live-update-ingestion/02-03-SUMMARY.md`
</output>
